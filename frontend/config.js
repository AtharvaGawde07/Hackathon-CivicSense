/**
 * CivicAI — Frontend Configuration
 *
 * Centralised backend URL.  Change this single value when deploying
 * to a different host or port — never hardcode the URL inline.
 */
const API_BASE_URL = "http://127.0.0.1:8000";
